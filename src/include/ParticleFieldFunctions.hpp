#ifndef __PARTICLE_FIELD_FUNCTIONS
#define __PARTICLE_FIELD_FUNCTIONS

#include "vec.hpp"

using Angel::vec3;
using Angel::vec4;

namespace ParticleFieldFunctions
{

  vec3 tornado(vec4);
  vec3 flame(vec4);
  vec3 fixed(vec4);
  vec3 up(vec4);

}

#endif
